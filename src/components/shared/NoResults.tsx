import React from "react";

const NoResults: React.FC = () => (
  <div className="no-results">
    <h3>No results found</h3>
    <p>Try searching for a different one!</p>
  </div>
);

export default NoResults;
